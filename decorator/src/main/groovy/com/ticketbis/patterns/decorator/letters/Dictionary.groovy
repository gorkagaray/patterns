package com.ticketbis.patterns.decorator.letters

import com.ticketbis.patterns.decorator.player.Player

class A extends Letter {

    Player gameState

    A() {}

    A(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}


class B extends Letter {

    Player gameState

    B() {}

    B(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 3
    }
}


class C extends Letter {

    Player gameState

    C() {}

    C(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 3
    }
}


class D extends Letter {

    Player gameState

    D() {}

    D(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 2
    }
}


class E extends Letter {

    Player gameState

    E() {}

    E(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}


class F extends Letter {

    Player gameState

    F() {}

    F(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 4
    }
}


class G extends Letter {

    Player gameState

    G() {}

    G(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 2
    }
}


class H extends Letter {

    Player gameState

    H() {}

    H(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 4
    }
}


class I extends Letter {

    Player gameState

    I() {}

    I(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}


class J extends Letter {

    Player gameState

    J() {}

    J(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 8
    }
}


class K extends Letter {

    Player gameState

    K() {}

    K(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}


class L extends Letter {

    Player gameState

    L() {}

    L(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}


class M extends Letter {

    Player gameState

    M() {}

    M(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 3
    }
}


class N extends Letter {

    Player gameState

    N() {}

    N(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}

class Ñ extends Letter {

    Player gameState

    Ñ() {}

    Ñ(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 8
    }
}


class O extends Letter {

    Player gameState

    O() {}

    O(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}


class P extends Letter {

    Player gameState

    P() {}

    P(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 3
    }
}


class Q extends Letter {

    Player gameState

    Q() {}

    Q(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 5
    }
}


class R extends Letter {

    Player gameState

    R() {}

    R(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}


class S extends Letter {

    Player gameState

    S() {}

    S(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}


class T extends Letter {

    Player gameState

    T() {}

    T(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}


class U extends Letter {

    Player gameState

    U() {}

    U(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 1
    }
}


class V extends Letter {

    Player gameState

    V() {}

    V(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 4
    }
}


class W extends Letter {

    Player gameState

    W() {}

    W(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 100000 // ;)
    }
}


class X extends Letter {

    Player gameState

    X() {}

    X(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 8
    }
}


class Y extends Letter {

    Player gameState

    Y() {}

    Y(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 4
    }
}


class Z extends Letter {

    Player gameState

    Z() {}

    Z(Player gameState) {
        this.gameState = gameState
    }

    Integer getValue() {
        gameState.getValue() + 10
    }
}